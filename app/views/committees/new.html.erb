<h1>New committee</h1>

<%= render 'form' %>

<%= link_to 'Back', committees_path %>

<%= form_for @committee, :url => committee_path, :html => { :multipart => true } do |form| %>
    <%= form.file_field :avatar %>
<% end %>